# 作者参考标定说明

原作者的参考标定文件位于 `../Original/shape_reconstruction/calibration`。该路径以
`custom_9dtact` 项目根目录为起点，实际内容保存在 GitHub 仓库根目录的只读
`Original/` 快照中。

Sensor 1 和 Sensor 2 是两个自制传感器，必须分别完成 Camera Calibration 和
Sensor Calibration，不能直接使用原作者的标定结果，也不能互相复制标定结果。
